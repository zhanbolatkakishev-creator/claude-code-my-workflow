# DCAS Compliance Checklist — "Corridor, Not Factory"

Against the [AEA Data and Code Availability Standard](https://datacodestandard.org/) and
openICPSR deposit expectations. `PASS` = met; `FAIL` = must fix before deposit; `[FILL]` =
author action, non-blocking for the skeleton.

| # | Requirement | Status | Note |
|---|---|---|---|
| 1 | **Data Availability Statement** present, per source | **PASS** | README §2; each of the six inputs classified public / licensed / published, redistribution stated |
| 2 | Every dataset has **source + access + license** | **PASS** | README §3 manifest, one row per file |
| 3 | **Master script**, one command | **PASS** | `kz_passthrough/00_run_all.R` and `kz_valueadd/00_run_all.R` (added 2026-09-02; runs 01–08, verified end-to-end) |
| 4 | **Computational requirements** stated (OS, software, versions, runtime, RAM) | **PASS** (runtime `[FILL]`) | `output/computational_requirements.md`, `renv.lock`, two `sessionInfo.txt`. Runtime line marked for author confirmation |
| 5 | **Every table and figure mapped** to program : line | **PASS** | README §6 — 6 body tables, 5 figures, 4 appendix tables, plus the load-bearing in-text numeric claims |
| 6 | **No absolute / machine-specific paths** in code | **PASS** (fixed 2026-09-02) | Added `kz_valueadd/00_setup.R` with relative `DIR_DATA` / `DIR_OUT` / `DIR_PASS_OUT` / `DIR_PASS_DATA`; all 8 `kz_valueadd` scripts refactored to `source("00_setup.R")` and relative paths (was 14 absolute-path lines across 8 files, incl. the OneDrive `SRC`). `01_read_deals.R` reads the licensed xlsx from `_data/` or `$KZ_DEAL_XLSX`. `kz_passthrough` was already relative. Verified: `kz_valueadd/00_run_all.R` runs end-to-end. |
| 7 | **Seeds set** for every stochastic step | **PASS** | `06_eventstudy_did.R` and `12_did_robustness.R`: `set.seed(20260831)`; `kz_valueadd/08_power_null.R`: `set.seed(1)`. `10_robustness.R` and the I-O / fiscal steps are deterministic (sup-F tests, linear algebra). |
| 8 | **License** — a code license + a data-usage statement | **PASS** | `LICENSE.md`: MIT for the replication code (© 2026 Zhanbolat Kakishev) + a per-source data-usage statement |
| 9 | **Reproducibility verified** (numbers reproduce to tolerance) | **PASS** | `quality_reports/reproducibility_audit_corridor.md`: 118 PASS / **0 FAIL** / 1 EXPLAINED / 3 UNMATCHED; every post-audit revision re-verified incrementally, all PASS. The one earlier FAIL (§5.2 under-invoicing 15–26 % → 13–21 %) was corrected in-audit. |
| 10 | **Restricted-data handling** — pointer, not the data; code still ships | **PASS** | `data/raw/access-restricted-data.md`; the analysis code runs unchanged once a licence holder supplies the xlsx; only aggregate counts (Table 3) are shipped, no deal-level rows |
| 11 | **Provenance** — constructed files traceable to raw + script | **PASS** | README §3 "In package?" column + §6 map; `_data/` raw pulls regenerated by `fetch_*.sh` |
| 12 | **Deposit metadata** (title, authors, JEL, openICPSR project) | **[FILL]** | Author to enter at upload; JEL from the manuscript (F14, F15, E22, D25, O14, O24, O33, P33) |

## Blocking items

**All three blocking items were cleared on 2026-09-02:**

1. ~~**#6 absolute paths**~~ — `kz_valueadd/00_setup.R` added; all 8 scripts refactored to
   relative paths; `kz_valueadd/00_run_all.R` verified end-to-end.
2. ~~**#3 master script** for `kz_valueadd/`~~ — `kz_valueadd/00_run_all.R` added.
3. ~~**#8 code license**~~ — `LICENSE.md` (MIT, © 2026 Zhanbolat Kakishev).

Remaining items are all PASS or author-metadata (`#12` openICPSR project title / JEL at
upload; the runtime line in `computational_requirements.md`; and the deal-ID list, which the
author must clear with the three vendors before publishing even bare identifiers).
