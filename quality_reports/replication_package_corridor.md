# Replication Package: "Corridor, Not Factory: Trade Reorientation and the Missing Investment Response in Kazakhstan, 2022–2025"

**Date:** 2026-09-02 · **Language:** R 4.5.3 (only) · **Deposit target:** openICPSR (Elsevier / *Journal of International Economics* data editor)

## DCAS checklist

| Item | Status |
|---|---|
| Data Availability Statement | **PASS** — README §2; every source classified public / licensed, redistribution stated |
| Dataset manifest (source · access · license) | **PASS** — README §3, one row per input |
| One-command master script | **PASS** (fixed 2026-09-02) — `kz_passthrough/00_run_all.R` and `kz_valueadd/00_run_all.R`; the latter added and verified end-to-end |
| Computational requirements | **PASS** — `renv.lock` + `scripts/R/_outputs/computational_requirements.md` + two `sessionInfo.txt`; runtime line marked `[author to confirm]` |
| Table/Figure → program:line map | **PASS** — README §6, all 6 body tables + 5 figures + 4 appendix tables mapped |
| No machine-specific paths in code | **PASS** (fixed 2026-09-02) — added `kz_valueadd/00_setup.R` (relative `DIR_*`); refactored all 8 `kz_valueadd` scripts (14 absolute-path lines removed, incl. the OneDrive `SRC`); `01_read_deals.R` reads from `_data/` or `$KZ_DEAL_XLSX`. Also neutralised two loaded-vocabulary code comments. |
| Seeds set for stochastic steps | **PASS** — `set.seed(20260831)` in `06_eventstudy_did.R` and `12_did_robustness.R`; `set.seed(1)` in `kz_valueadd/08_power_null.R`; all other steps deterministic |
| License (code + data-usage statement) | **PASS** (fixed 2026-09-02) — `replication_package/LICENSE.md`: MIT for the replication code (© 2026 Zhanbolat Kakishev) + per-source data-usage statement |
| Reproducibility audit (Phase 3) | **PASS** — `quality_reports/reproducibility_audit_corridor.md`: 118 PASS / **0 FAIL** / 1 EXPLAINED / 3 UNMATCHED, with every post-audit revision (R2 must-fix, secondary items, verify-claims fixes) incrementally re-verified, all PASS |
| Confidential-data note | **PASS** — `replication_package/data/access-restricted-data.md` (the Capital IQ / PitchBook / Preqin deal extract) |

## Skeleton built at

`replication_package/` — `README.md`, `DCAS_checklist.md`, `data/access-restricted-data.md`,
`LICENSE.md` (`[FILL]`). The `code/` and `output/` directories are **pointers** to the live
`scripts/R/kz_passthrough/`, `scripts/R/kz_valueadd/`, and their `_outputs/` — the README §7
gives the copy commands to populate the openICPSR tree (excluding `_data/`, which is ~620 MB
of raw pulls and is regenerated by the `fetch_*.sh` scripts).

## Blocking items — all cleared 2026-09-02

1. ✅ `scripts/R/kz_valueadd/00_run_all.R` added (runs 01–08; verified end-to-end).
2. ✅ `scripts/R/kz_valueadd/00_setup.R` added; all 8 scripts refactored to relative paths
   (14 absolute-path lines removed). `01_read_deals.R` now reads the licensed xlsx from
   `_data/` (or `$KZ_DEAL_XLSX`).
3. ✅ `replication_package/LICENSE.md` finalised — MIT (© 2026 Zhanbolat Kakishev).

## Remaining [FILL] items (non-blocking; author at upload)

1. Confirm the approximate end-to-end runtime for the "Computational requirements" block.
2. Confirm the openICPSR project title, and whether the deal-ID list may be published in the
   package or must sit behind the same academic-licence wall (README §2 assumes it may be
   published as identifiers only, no deal-level fields).
3. OECD ICIO 2023 edition — the license permits research use but not redistribution of the
   raw matrix; README §3 points to the OECD download rather than shipping `_data/icio/`.
